{
  "config": {
    "step": {
      "user": {
        "title": "Set up RV-C",
        "description": "Configure MQTT options for RV-C integration.",
        "data": {
          "topic_prefix": "MQTT topic prefix",
          "auto_discovery": "Enable auto discovery"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "RV-C options",
        "description": "Fine-tune MQTT topics and availability monitoring.",
        "data": {
          "topic_prefix": "MQTT topic prefix",
          "auto_discovery": "Enable auto discovery",
          "command_topic": "Command topic",
          "gps_topic": "GPS topic",
          "availability_timeout": "Availability timeout (seconds)"
        }
      }
    }
  },
  "title": "RV-C Integration"
}
